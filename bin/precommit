#!/usr/bin/env ruby
require "fileutils"

APP_ROOT = File.expand_path("..", __dir__)

def system!(*args)
  system(*args, exception: true)
end

FileUtils.chdir APP_ROOT do
  system! "bin/fmt"
  # TODO: put these back when we're done tinkering
  # system! "bin/rails test"
end
