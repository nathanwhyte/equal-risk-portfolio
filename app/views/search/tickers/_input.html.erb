<%= form_with url: search_tickers_path, method: :get, local: true, class: "flex gap-4 items-center" do |form| %>
  <div class="flex gap-2 input">
    <%= render "shared/icons/search" %>
    <%= form.text_field :query,
                    value: params[:query],
                    placeholder: "Search tickers...",
                    autocomplete: "off" %>
    <kbd class="kbd kbd-md">↵</kbd>
  </div>

  <%= button_tag(type: "submit", class: "btn btn-md btn-primary") do %>
    Search
  <% end %>
<% end %>
